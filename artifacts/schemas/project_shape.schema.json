{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProjectShape",
  "description": "Reusable directory-layout + role-ownership preset under policies/project-shapes/. Picked by the architecture agent when writing team_plan.json#project_shape.",
  "type": "object",
  "required": ["name", "description", "ownership"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$",
      "description": "Filename-safe slug. Must equal the JSON file's basename without extension."
    },
    "description": {
      "type": "string",
      "minLength": 20,
      "description": "When to pick this shape — one or two sentences."
    },
    "ownership": {
      "type": "object",
      "minProperties": 1,
      "description": "Per-role path globs that may be written by that role. Globs use `**` for recursive match. Roles MUST be a subset of those declared in policies/agents.config.json#default_ownership.",
      "additionalProperties": {
        "type": "array",
        "minItems": 1,
        "items": { "type": "string" }
      }
    },
    "notes": {
      "type": "string",
      "description": "Free-text guidance for the architecture agent."
    }
  }
}
